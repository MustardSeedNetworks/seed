{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/krisarmstrong/seed/main/docs/schemas/api/setup-complete.schema.json",
  "$ref": "#/$defs/SetupCompleteRequest",
  "$defs": {
    "SetupCompleteRequest": {
      "properties": {
        "password": {
          "type": "string"
        },
        "setupToken": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "password",
        "setupToken"
      ]
    }
  },
  "title": "SetupCompleteRequest",
  "description": "SetupCompleteRequest — generated from the Go struct in internal/api; refresh with `make schema` after struct changes."
}
