{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/krisarmstrong/seed/main/docs/schemas/api/wifi-connect.schema.json",
  "$ref": "#/$defs/WiFiConnectRequest",
  "$defs": {
    "WiFiConnectRequest": {
      "properties": {
        "ssid": {
          "type": "string"
        },
        "password": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ssid"
      ]
    }
  },
  "title": "WiFiConnectRequest",
  "description": "WiFiConnectRequest — generated from the Go struct in internal/api; refresh with `make schema` after struct changes."
}
