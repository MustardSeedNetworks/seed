{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/MustardSeedNetworks/seed/main/docs/schemas/api/snmp-settings-response.schema.json",
  "$ref": "#/$defs/SNMPSettingsResponse",
  "$defs": {
    "SNMPSettingsResponse": {
      "properties": {
        "timeout": {
          "type": "integer"
        },
        "retries": {
          "type": "integer"
        },
        "port": {
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "timeout",
        "retries",
        "port"
      ]
    }
  },
  "title": "SNMPSettingsResponse",
  "description": "SNMPSettingsResponse — generated from the Go struct in internal/api; refresh with `make schema` after struct changes."
}
