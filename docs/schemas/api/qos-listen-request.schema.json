{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/MustardSeedNetworks/seed/main/docs/schemas/api/qos-listen-request.schema.json",
  "$ref": "#/$defs/ListenRequest",
  "$defs": {
    "ListenRequest": {
      "properties": {
        "port": {
          "type": "integer"
        },
        "family": {
          "type": "string",
          "enum": [
            "ipv4",
            "ipv6"
          ]
        },
        "durationSeconds": {
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "port"
      ]
    }
  },
  "title": "ListenRequest",
  "description": "ListenRequest — generated from the Go struct in internal/api; refresh with `make schema` after struct changes."
}
