{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/MustardSeedNetworks/seed/main/docs/schemas/api/traceroute-request.schema.json",
  "$ref": "#/$defs/TracerouteRequest",
  "$defs": {
    "TracerouteRequest": {
      "properties": {
        "target": {
          "type": "string"
        },
        "protocol": {
          "type": "string"
        },
        "port": {
          "type": "integer"
        },
        "maxHops": {
          "type": "integer"
        },
        "timeout": {
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "target",
        "protocol",
        "port",
        "maxHops",
        "timeout"
      ]
    }
  },
  "title": "TracerouteRequest",
  "description": "TracerouteRequest — generated from the Go struct in internal/api; refresh with `make schema` after struct changes."
}
